# Critical Notes
Simplicity is the main contribution. Shows CoT length is strongly correlated with success if the model is 'forced' to overthink.

## Limitations
Token cost scales linearly with forced 'thinking' time.

## Extension Ideas
Dynamic budget forcing using uncertainty (entropy) rather than fixed token counts or difficulty classifiers.
